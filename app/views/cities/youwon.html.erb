<div class="tts-apps">
	<h1>You did it!</h1>
	<h3>You named all cities where TTS has a Campus!</h3>

	<p>Here they are:</p>

	<div>
		<% @cities.each do |city| %>
			<%= "#{city.id}. #{city.name}" %><br />
		<% end %>
	</div>

	<div>
			<%= link_to "Take Another Quiz", quizzes_path %>
	</div>
</div>

<script>
document.getElementById("topbar").style.display = "none";
</script>